<%= link_to gravatar_for(current_user, size: 52), current_user %>
<h2>
  <%= current_user.name %>
</h2>
<span>
  <%= link_to "view my profile", current_user %>
</span>
<span>
  <%= pluralize(current_user.sighting_records.count, "sighting record") %>
</span>